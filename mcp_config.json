{
  "mcpServers": {
    "token-optimizer": {
      "command": "uvx",
      "args": [
        "--from",
        "${pluginDir}",
        "python",
        "-m",
        "token_optimizer.server"
      ]
    }
  }
}
